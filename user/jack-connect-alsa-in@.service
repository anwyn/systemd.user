[Unit]
Description=Jack capture ports from ALSA card %I via zita-ajbridge
BindsTo=jack.service
After=jack.service

[Service]
ExecStart=/usr/bin/zita-a2j -j "%I" -d "%I" -c 2 -r ${JACK_RATE} -p ${JACK_PERIOD} -n ${JACK_NPERIODS}

[Install]
WantedBy=sound.target
